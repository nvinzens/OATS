hosts:
  - host: 10.20.1.22
    port: 830
    username: ins
    password: ins@lab
    subscriptions:
        - subscription:
            xpath: /if:interfaces-state/interface/statistics
            period: 1000
            kafka_publish_topic: interfaces-out-discards
