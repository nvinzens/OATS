auto_accept: True # Don't use this in production.
extension_modules: /etc/salt/ext
log_level_logfile: debug
file_roots:
  base:
    - /etc/salt/master #needs to point to top.sls
pillar_roots:
  base:
    - /pillar
